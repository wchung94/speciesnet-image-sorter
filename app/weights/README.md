### Description
This folder contains the local weights of speciesnet and megadetector downloaded from kaggle.
These models weights are necessary if one wants to run this application locally.

## How to download
To download the weights to this local directory:
run the file:
`python download_weights.py`